#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_pushstate-server@3.0.1@pushstate-server/bin/pushstate-server" "$@"
  ret=$?
else 
  node  "$basedir/../_pushstate-server@3.0.1@pushstate-server/bin/pushstate-server" "$@"
  ret=$?
fi
exit $ret
